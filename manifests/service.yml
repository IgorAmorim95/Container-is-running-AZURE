apiVersion: v1
kind: Service
metadata:
    name: igoramorimcontainerisrunningazure
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: igoramorimcontainerisrunningazure